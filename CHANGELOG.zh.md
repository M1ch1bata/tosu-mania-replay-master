# Mania Replay Master v0.5.1 更新说明

本次更新主要解决一个老问题：**同一排音符的判定颜色经常与实际按键对不上**。同时将“边玩边分析”和“看回放分析”完全拆分为两条独立流程，并让精确辅助进程（helper）变为双击即用、用完自动退出。

> **精确辅助（helper）现在双击即可使用：**
> `tools\start-helper.bat` 启动，`tools\stop-helper.bat` 关闭；自动识别 osu! 安装目录，跟随 osu! / tosu 自动退出。**5K–10K 建议务必开启**，详见下方「精确辅助（helper）使用方式（重点）」。

## 本次重点

- **同排音符不再串位**
  修复了按键“按下 / 抬起”被反向处理的问题：此前按键按下时匹配不到音符，判定实际发生在松键瞬间，且按住时长被计入误差，导致和弦颜色混乱、按下的轨道反而显示 MISS。现在按下判定音符头、抬起判定长条尾，与 osu! 客户端一致。

- **按键只命中该列最早的未判定音符**
  与客户端的音符锁规则一致，不会再将本次按键算到相邻或后续音符上，连打与叠键场景更可靠。

- **按键时间更准确**
  使用更准确的按键时间（键盘事件时间，并用游戏时钟校准），MAX / 300 的判定颜色不再因延迟随机变化。

- **DT / HT 判定窗口修正**
  加速与减速模式下的判定窗口按实际倍率计算，stable 与 lazer 均与游戏一致。

- **实时游玩不再套用上一局回放**
  此前实时游玩时，插件可能套用上一次的回放数据，导致按键被忽略、判定错乱。现在：
  - 边玩边分析：仅使用实时按键，不读取回放文件；
  - 看回放分析：仅读取本局回放文件，进入游戏即预渲染整局判定，不连接键盘。

- **重播 / 倒带统计归零**
  成绩面板与 UR 随回放进度增长，重新播放或拖动进度条时不再残留上一轮的最终数据。

- **游戏退出后的状态修复**
  osu! 关闭后，插件不再停留在“游戏中”状态空转。

## 新增分析模式

tosu 设置中新增 `Analysis Mode`：

| 模式 | 适用场景 |
| --- | --- |
| Auto（默认） | 自动切换：检测到实时按键时按边玩处理，否则按回放处理 |
| Live | 固定实时判定，适合练图与正常游玩 |
| Replay | 固定回放预渲染，适合复盘录像 |

不确定时保持 Auto；若 Auto 偶有误判，将其固定为当前实际场景即可。

## 精确辅助（helper）使用方式（重点）

**想要逐列精确判定（尤其是 5K–10K），请开启 helper。** 操作只有两步：

1. 双击 `tools\start-helper.bat` 启动。helper 隐藏运行，并**自动识别 osu! 安装目录**，无需填写路径；
2. 不用时双击 `tools\stop-helper.bat` 关闭；也可以完全不管它——它会监视 `osu!` 与 `tosu`，两者都启动过之后，任意一个关闭即自动退出（另有 10 分钟无人使用自动退出兜底）。

无需设置开机自启，也不会残留常驻后台进程。

osu! 不向第三方提供逐列按键数据，因此要让每个按键精确对应到轨道，必须由 helper 读取本机键盘输入。**5K–10K 建议务必开启 helper；4K 不开也可使用，但同排和弦偶尔可能归属到错误的列。**

## 注意事项

- 首次观看某个回放时为“边播边判”；完整观看一遍后，再次观看、循环或拖动进度条都会立即显示完整判定。
- 仅支持 osu!mania；选歌、菜单、结算界面会自动隐藏。
- helper 仅监听本机 `127.0.0.1`，支持访问令牌，不会向外部发送数据。

## 反馈

若判定颜色仍不正确，请附上：客户端（stable / lazer）、键数、mods、是否开启 helper、Analysis Mode，以及问题截图或录屏，以便定位。
